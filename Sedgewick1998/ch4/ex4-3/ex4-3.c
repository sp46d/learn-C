#include <stdio.h>
#include <string.h>
// For int type
typedef int Item;
#define eq(A, B) (A == B)
#define ITEMshow(ITEM) (printf("%d\n", ITEM))

// For string type
// typedef char* Item;
// #define eq(A, B) (strcmp(A, B) == 0)
// #define ITEMshow(ITEM) (printf("%s\n", ITEM))
